# Tailscale-only overlay: removes public port bindings.
# Usage: docker compose -f docker-compose.yml -f docker-compose.tailscale.yml up -d
services:
  labtether:
    ports: !reset []
  web-console:
    ports: !reset []
  postgres:
    ports: !reset []
